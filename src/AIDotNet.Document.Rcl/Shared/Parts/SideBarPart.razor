@using AIDotNet.Document.Rcl.Shared.Components.SideBars;


@if (SideBarType is SideBarType.Hidden)
{

}
else if (SideBarType is SideBarType.Mini)
{
    <MiniSideBar Width="54" Menus="menus" OnRefreshClick="RefreshClick" Clipped=Clipped />
}
else
{
    <FullSideBar Width="FullWidth" Menus="menus" OnRefreshClick="RefreshClick" Clipped=Clipped />
}
